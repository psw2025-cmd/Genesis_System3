Run a 100% self-directed, autonomous SWE agent loop on this repository targeting the live production deployment at [System3 Overview Dashboard](https://genesis-system3-web-doq2wplepa-el.a.run.app/ui?tab=overview).

You operate as a self-guiding Outer-Loop Engineering Team (Architect, Core Engineer, and QA Evaluator). You are given ZERO hardcoded bug descriptions. You must autonomously crawl the system, discover every functional, telemetry, visual, and architectural flaw on your own, and resolve them from the core codebase.

---

### Invariant Guardrails (`agent_policy.yaml`)
1. **Zero Synthetic Inventions:** Never fabricate fake prices, synthetic market quotes, or dummy metrics to artificially force cards to show green. Fallbacks must use authenticated snapshot buffers.
2. **Strict Risk & Gate Preservation:** Never weaken, bypass, or lower thresholds for safety gates, risk limits, or trade execution authority locks.
3. **Test-First (Eval Specs):** For every issue you independently discover, write a failing regression/acceptance test in `tests/evals/` first. Only commit and push when your test passes.

---

### Step 1: Autonomous System Crawl & Issue Discovery
- Use your headless browser, API probes, and DOM inspection tools to crawl all routes across the application:
  `/ui?tab=overview`, `decision-intel`, `truth-control`, `genesis-brain`, `e2e-proof`, `options-intel`, `option-chain`, `signals`, `trade`, `paper-trades`, `positions`, `multibagger`, `prediction-audit`, `performance`, `ml-model`, `data-integrity`, `broker`, `alerts`, `system`, `live-gate`.
- Deeply inspect all backend APIs (`/api/state`, `/api/health`, `/api/proof_gates`, `/api/contracts`, `/api/options`, etc.).
- Dynamically build your own prioritized backlog by identifying:
  - Any tripped, blocked, or unproven proof gates.
  - Telemetry contract mismatches, schema conflicts, or missing backend dependencies.
  - Inactive/unbound model prediction pipelines, confidence calculations, or historical feature evaluators.
  - Frontend visual collisions (text overlaps, broken flexbox/CSS cards, misaligned numbers, raw error strings).
  - Unhandled exceptions, stale caches, and broker fallback handlers.

---

### Step 2: Live Progress Telemetry (Immediate Deploy)
- Inject a live diagnostic telemetry banner at the top of the UI rendering:
  - `[AUTONOMOUS LOOP] | Active Task: <Self-Discovered Subsystem> | Progress: <Resolved Count>/<Total Identified> | Gates: <X/7> | Build: <Git SHA>`
- Build and push this initial telemetry change to Google Cloud Run immediately so all subsequent autonomous progress is verifiable live.

---

### Step 3: Sequential Self-Healing & Continuous Deployment
Iteratively work through your self-generated issue queue:
1. **Write Acceptance Test:** Implement a failing spec in `tests/evals/` capturing the discovered defect.
2. **Core Architectural Fix:** Apply root-cause fixes to backend pipelines, math engines, broker adapters, or UI stylesheets.
3. **Local Gate Verification:** Run `pytest tests/evals/` locally to verify clean passes.
4. **GitOps Release:** Commit changes (`git commit -am "fix(core): resolved <discovered_component>"`) and push to `main` to trigger deployment to Google Cloud Run.
5. **Live Verification:** Poll the deployed URL, confirm the new `UI Rendered SHA`, and visually verify the target card, table, or gate flips from broken/waiting to clean active values.
6. **Next Item:** Re-crawl the live app to detect newly unblocked dependencies, update the telemetry banner progress, and proceed immediately.

---

### Step 4: Clean Production Handover
- When you independently verify that zero unhandled issues remain across all tabs and all proof gates report `READY`:
  - Cleanly remove the temporary diagnostic telemetry banner.
  - Run full regression test suites to guarantee stability.
  - Execute the final production deployment to Google Cloud Run.
  - Print a comprehensive audit report in the terminal listing every self-discovered issue, acceptance test created, and architectural fix applied.