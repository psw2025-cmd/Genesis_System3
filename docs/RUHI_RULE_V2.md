# RHUI_RULE_V2.2 LOCAL-LAPTOP

Scope: `psw2025-cmd/Genesis_System3` only.

- GitHub current `main` is the only code authority.
- The authorized Windows laptop checkout `C:\Genesis_System3_Clean` is the only execution/runtime and acceptance authority.
- Issue #188 is the canonical shared coordination bus; Gmail is secondary asynchronous evidence.
- Start every verification from fresh remote main, active PR ownership, current laptop provenance, and the localhost URL/port defined by current-main configuration.
- Never promote stale evidence to current health.
- Broker acceptance requires current-main local `/ui` connected state plus same-session API and fresh NIFTY/BANKNIFTY/FINNIFTY/MIDCPNIFTY semantic proof.
- Never expose broker secrets or authentication factors.
- `LIVE_TRADING_ENABLED=0`, `SYSTEM3_LIVE_TRADING_ALLOWED=0`, `AUTO_EXECUTE_TRADES=0`; real broker order count remains zero.
