# Genesis System3 — Cursor thin pointer

Authoritative constitution: `AGENTS.md`.
Machine invariants: `agent_policy.yaml`.
Universal Cursor rule: `.cursor/rules/00-repository-safety.mdc`.
Temporal marker: `SYSTEM3_TEMPORAL_TRUTH_V1` (`docs/authority/TEMPORAL_TRUTH_AND_LIVE_EVIDENCE_POLICY.md`).

Canonical checkout: `C:\Genesis_System3_Clean`.
Runtime: local Windows `http://127.0.0.1:8000` (PAPER/ANALYZE).
Broker: Dhan. GCP Cloud Run, Claude-only ownership, Issue #188 as a live bus, Angel, and Render hosting are retired and non-authoritative.

Safety: `ANALYZE_MODE=1`, `LIVE_TRADING_ENABLED=0`, `SYSTEM3_LIVE_TRADING_ALLOWED=0`, `AUTO_EXECUTE_TRADES=0`. Never place a real order. Never expose secrets. Never reset a dirty worktree.
