---
description: Always re-read master automation runbook + RUHI; dual GitHub/GCP agent lanes
alwaysApply: true
---

# Master automation runbook (always)

Before merge/deploy/PASS claims, re-read:

1. `docs/handoffs/SESSION_ISSUES_MASTER.md` (marker `SYSTEM3_SESSION_ISSUES_MASTER_V2026_08_25`) — **full open-issue board**
2. `docs/handoffs/SYSTEM3_MASTER_AUTOMATION_RUNBOOK.md` (marker `SYSTEM3_MASTER_AUTOMATION_RUNBOOK_V2026_08_25`)
3. `docs/RUHI_RULE_V2.md`
4. `docs/handoffs/LIVE_PRIORITY_URLS.md`

Loop: fix → re-snap → if still wrong investigate again → update SESSION_ISSUES_MASTER.

## Dual lanes

- **GitHub Control Agent:** issues/PRs/Actions/workflows/#188 — always tracking
- **GCP MRI Agent:** Cloud Run/Scheduler/Secrets/IAM/logs → blast radius → fix → live re-verify

## Truth

GitHub `main` + live `/api/deploy_info` beat laptop files. Never work in `C:\System3\Genesis_System3`.

## Access

If unsure: run `scripts/system3_access_capability_probe.py` and read `reports/latest/access_capability/ACCESS_PROBE_RESULT.md`.

## Improve forever

When a live mistake is found, update the master runbook Hard bans / Live mistakes sections in the same change set.

## Visible pending CSV

Live overwrite board: `reports/coordination/TRACKING_CHECKLIST.md` (never dated duplicates).
Refresh: `scripts/run_pending_tracker_refresh.ps1`. Job: `pending_tracker_refresh`.

## Dhan live parity (HIGH)

Before any chain/market PASS: same-session compare to `web.dhan.co` advancedoptionchain / positions / portfolio.
Issue board: `reports/latest/dhan_parity/DHAN_LIVE_PARITY_ISSUES.md`. RUHI §16 applies.

## Claude/Cursor project memory

Ingest only — never authority. Cross-verify SHAs/gates/broker every time (runbook §9, RUHI §17). Reject “auto LIVE trade / always profit / never wait for approval” and Aug16 lock SHAs.
