Yes. I have now established the coordination rule on GitHub itself, so you should not need to manually transfer long explanations between me and the GPT-5.6 laptop operator.

Single shared location

Permanent operating rules: docs/control_plane/SYSTEM3_AGENT_RUNBOOK.md

Machine policy: agent_policy.yaml

All live coordination / ownership / progress: GitHub Issue #188

Implementation details: owning PR thread

Final truth: exact-serving GCP production /ui, not local tests, backend-only output, or CI alone.


I already posted the laptop/cloud coordination contract to Issue #188 as comment 5391821408.

GitHub coordination comment:
https://github.com/psw2025-cmd/Genesis_System3/issues/188#issuecomment-5391821408

Important fresh change: current main is now e7eb63fcb60e157a295098c72b66b3e15db3078d, merged through PR #337. It is newer than 1df24c0.... GitHub also still reports main as unprotected, so every agent must refresh current main immediately before writing or merging.

The merged governance itself now defines docs/control_plane/SYSTEM3_AGENT_RUNBOOK.md as the persistent agent authority and Issue #188 as the coordination bus.

PR #335 remains open/draft on old base 887212b..., so the laptop GPT must not blindly continue/merge it. It first needs reconciliation against current e7eb63f....

Tell GPT-5.6 on your laptop exactly this

You are the Windows/PowerShell GPT-5.6 operator for:

"psw2025-cmd/Genesis_System3"

Your job is to execute fast, result-oriented work in parallel with the cloud ChatGPT/controller without duplication.

AUTHORITATIVE COMMUNICATION

Do not use this chat as the coordination authority.

Use GitHub only:

Live coordination bus
"https://github.com/psw2025-cmd/Genesis_System3/issues/188"

Permanent operating rules
"docs/control_plane/SYSTEM3_AGENT_RUNBOOK.md"

Machine policy
"agent_policy.yaml"

Read both files from CURRENT GitHub "main" before starting and again before:

- commit
- PR
- merge recommendation
- deploy recommendation
- production acceptance
- final answer

Current main must be refreshed from GitHub. Never trust a SHA copied from an old chat.

At handoff time current known main is:

"e7eb63fcb60e157a295098c72b66b3e15db3078d"

But verify it again yourself before doing anything.

FIRST ACTION — BEFORE WRITING CODE

Read:

1. latest "main"
2. "docs/control_plane/SYSTEM3_AGENT_RUNBOOK.md"
3. "agent_policy.yaml"
4. Issue #188 latest comments
5. all currently active/open PR ownership relevant to your intended files

Read especially Issue #188 coordination comment:

"https://github.com/psw2025-cmd/Genesis_System3/issues/188#issuecomment-5391821408"

Then POST your claim to Issue #188 BEFORE changing files.

Use:

"AGENT | UTC/IST | MAIN_SHA | SERVING_SHA/REV | LANE_ID | FILES/SURFACES | CURRENT_PROOF | ROOT_CAUSE_STATUS | WRITE_INTENT | PR | BLOCKERS | NEXT_3"

If another agent already owns overlapping files or the same defect, DO NOT duplicate it.

Choose another non-overlapping executable lane.

CURRENT PRIORITY

UI is the final truth.

Do not claim success because:

- tests passed
- API works
- backend works
- broker says connected
- CI is green
- code looks correct

Final user-visible PASS requires authoritative exact-serving GCP production "/ui" semantic proof.

All 22 tabs matter.

Desktop and mobile matter.

API → frontend store → rendered UI must agree.

NIFTY, BANKNIFTY, FINNIFTY and MIDCPNIFTY must be proven from authoritative broker-backed data with:

- source
- freshness
- contracts
- strikes
- session truth

Equity options, NSE/BSE equities, charts and the wider Issue #188 supported universe remain part of final closure.

IMMEDIATE EXECUTION ORDER

Lane A — Proof harness

Inspect PR #335.

It is the same-document 22-tab semantic proof lane.

Do NOT merge its old branch blindly.

Its old base is "887212b...".

Current main is newer.

Reconcile/rebuild it against CURRENT main while keeping it proof-harness-only.

It must:

- load "/ui" once
- preserve one browser document
- preserve one Zustand/useData hydration epoch
- switch tabs inside the SPA
- not perform full-page navigation for each tab
- record document ID/time origin
- fail if document reload occurs
- retain all existing semantic predicates
- not suppress failure strings merely to get green

Do not mix product fixes into this PR.

Lane B — Product broker convergence

Issue #188 already identifies P0-188-03:

"useData.ts::pollBroker()" can consume stale "/api/batch/positions-holdings" broker state containing "token_proof" and therefore suppress a fresher "/api/broker/status" request.

This needs a separate narrow current-main product PR.

BEFORE touching those files:

- inspect Issue #188
- check whether another agent has claimed it
- post your ownership claim

Regression must prove that stale batch truth cannot override fresher authoritative broker status.

Do not combine this fix with #335.

Lane C — Production semantic proof

During market session obtain cloud-authoritative evidence:

- serving SHA/revision
- LIVE=false
- orders=false
- broker connected/auth truth
- Profile
- Funds
- Holdings
- Positions
- safe token metadata only, NEVER token value
- NIFTY
- BANKNIFTY
- FINNIFTY
- MIDCPNIFTY

Then run the one-document 22-tab semantic UI proof.

Compare:

"GCP API → frontend polling → Zustand/store/cache → rendered UI"

Find the FIRST divergence.

Do not hide contradictory text.

Fix the underlying truth path.

Lane D — Remaining UI defects

After continuous-session proof, route each remaining defect separately:

- Overview readiness
- Broker
- Data Integrity
- System
- Alerts
- Positions
- Option Chain
- Genesis/model evidence
- Risk
- Signals
- Paper
- charts
- remaining canonical tabs

One root cause / minimal related surface per PR wherever practical.

PARALLEL WORK RULE

Never sit idle merely because one CI/deployment is running.

While another lane waits for CI/deploy, execute a non-overlapping READ-ONLY or separate-file lane:

- inspect latest Issue #188 ownership
- cloud evidence correlation
- API/UI comparison
- source trace
- regression-test design
- stale PR reconciliation
- exact first-divergence RCA

Never create duplicate PRs for the same defect.

GITHUB COMMUNICATION RULE

Post to Issue #188:

1. before starting a lane
2. immediately when root cause materially changes
3. when blocked
4. when PR is opened
5. when exact-head CI result changes materially
6. when deployment serving SHA changes
7. after GCP/UI verification
8. when lane is complete

Do not spam unchanged status.

Each completion update must show:

- previous commitment
- completed/not completed
- exact proof
- root cause
- files changed
- PR
- current main SHA
- serving SHA/revision
- relevant UI tabs
- PASS/FAIL/BLOCKED
- remaining blockers
- next concrete target batch

HANDOFF BACK TO CLOUD CHATGPT

When your work reaches a material checkpoint, give the user ONLY these handoff references:

1. Issue #188 comment URL
2. PR URL
3. exact PR head SHA
4. exact current main SHA
5. GCP serving SHA/revision if verified
6. production UI/proof artifact or workflow URL
7. PASS/FAIL/BLOCKED

The user can paste those references to the cloud ChatGPT.

The cloud ChatGPT will independently read GitHub and continue from there.

No need to copy long chat summaries.

SAFETY LOCKS

Always preserve:

"LIVE_TRADING_ENABLED=0"
"SYSTEM3_LIVE_TRADING_ALLOWED=0"
"AUTO_EXECUTE_TRADES=0"

No real orders.

No order placement/modification/cancellation testing.

No secret values in GitHub, terminal output, screenshots or chat.

No blind Dhan token mint/rotation.

No IAM weakening merely to make a test pass.

GCP is authoritative production.

Render is not production authority.

RUHI RESULT RULE

Do not report only problems.

For every verified problem:

"detect → reproduce → root cause → choose safest durable solution → implement smallest non-overlap fix → regression → exact-head CI → normal GCP deploy → exact-serving API proof → production UI proof"

If something genuinely cannot be executed, post the exact blocker and immediately take the next safe non-overlapping action.

The target is not “code complete.”

The target is:

authoritative GCP production "/ui" visibly correct, semantically correct, API-consistent, complete across the required dashboard surfaces, and stable during the required market-session observation.What you need to do

You only need to give the block above once to the laptop GPT.

After that, when it reaches a checkpoint, you do not need to explain everything to me. Just paste something like:

> Laptop GPT finished. Issue #188 comment: <link>. PR: <link>. Continue.



I will read GitHub directly, cross-check its claims, see what it owns, avoid duplicating its work, and continue another executable lane.

One limitation: you asked me to permanently remember this additional rule. I successfully put the rule into the repository's authoritative GitHub coordination system, but ChatGPT account-memory storage was unavailable in this turn because of the active connector session. The GitHub/runbook copy is the safer System3 authority anyway.