# CI Truth Reports

Runtime proof files are generated by GitHub Actions.

Do not trust committed stale runtime proof JSON/MD files.
The authoritative proof is the workflow artifact from the PR run.

Committed files in this folder are only workflow-before snapshots and documentation.
