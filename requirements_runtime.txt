# Core backend
fastapi>=0.109.1  # P1: ReDoS multipart fix
uvicorn[standard]==0.24.0

# Data processing
pandas==2.1.3
numpy==1.26.4
scikit-learn>=1.5.0  # P1.5: TfidfVectorizer data leakage fix
scipy==1.12.0

# P1: packaging 24 satisfies streamlit 1.37 (packaging<25,>=20) and wheel 0.46.2 (packaging>=24)
packaging>=24,<25
wheel==0.46.2  # P1: path traversal fix

# Validation & parsing
pydantic==2.12.5
pydantic-core==2.41.5
python-multipart==0.0.22  # P1: path traversal, ReDoS fixes
marshmallow==3.26.2  # P1: DoS fix

# File system monitoring
watchdog==3.0.0

# ML / AI stack (CPU builds for Python 3.10)
lightgbm==4.6.0  # P1: RCE fix
xgboost==2.0.3
torch>=2.6.0  # P1: torch.load RCE fix
tensorflow==2.15.0
protobuf==4.25.8  # P1: recursion DoS fix

# Visualization & dashboards
matplotlib==3.8.3
seaborn==0.13.2
altair==4.2.2
plotly==5.18.0
dash>=2.15.0,<3  # P1.5: XSS fix; pin to 2.x for compatibility
# flask/werkzeug: dash 2.x requires Flask<3.1, Werkzeug<3.1; blocked until dash 3.x
streamlit>=1.40,<1.55  # P1.5: path traversal fix; supports pillow 12.x for CVE-2026-25990

# CSV / Excel / Parquet
openpyxl==3.1.2
pillow>=12.1.1  # P1: CVE-2026-25990 PSD out-of-bounds write fix
xlrd==2.0.1
pyarrow>=17.0,<24  # P1: CVE-2024-52338 deserialization RCE fix

# Data ingestion / APIs
requests==2.32.4  # P1: TLS, .netrc leak fixes
aiohttp>=3.13.3  # P1.5: Request smuggling, DoS, info disclosure fixes
httpx==0.27.0
websockets==12.0

# Database & caching
SQLAlchemy==2.0.25
psycopg2-binary==2.9.9
redis==5.0.1
sqlite-utils==3.36
duckdb>=1.1,<2  # P1: CVE-2024-22682, CVE-2024-41672 extension/sniff_csv fixes

# Scheduling & background tasks
apscheduler==3.10.4
celery==5.3.6

# Security & config
cryptography>=42.0.5  # P1.5: X.509 name constraint, OpenSSL fixes
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
PyYAML==6.0.1

# libmagic handling (Windows-safe)
python-magic==0.4.27; platform_system != "Windows"
python-magic-bin==0.4.14; platform_system == "Windows"

# Locale & timezone
pytz==2025.2
tzlocal==5.2

# Editor agent support
rich==13.7.0
typer==0.24.1
loguru==0.7.2
